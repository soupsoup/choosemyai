{% extends "base.html" %}
{% block content %}
<h2 class="mb-4">Moderate Tools</h2>
{% if tools %}
<div class="row row-cols-1 g-4">
    {% for tool in tools %}
    <div class="col">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{ tool.name }}</h5>
                <h6 class="card-subtitle mb-2 text-body-secondary">
                    Category: {{ tool.category.name }} | Submitted by: {{ tool.author.username }}
                </h6>
                <div class="card-text mb-3">{{ tool.description|safe }}</div>
                
                <!-- Media section -->
                <div class="media-section">
                    <!-- Image display -->
                    {% if tool.image_url %}
                    <div class="mb-4">
                        <h6>Tool Image:</h6>
                        <div class="border rounded p-3">
                            <img src="{{ url_for('static', filename=tool.image_url) if tool.image_url.startswith('uploads/') else tool.image_url }}" 
                                 alt="Tool image" 
                                 class="img-fluid"
                                 onerror="this.onerror=null; this.src='{{ url_for('static', filename='images/placeholder.png') }}'; this.classList.add('error-image');">
                        </div>
                    </div>
                    {% endif %}
                    
                    <!-- YouTube display -->
                    {% if tool.youtube_url and tool.youtube_embed_url %}
                    <div class="mb-4">
                        <h6>YouTube Video:</h6>
                        <div class="border rounded p-3">
                            <div class="ratio ratio-16x9">
                                <iframe src="{{ tool.youtube_embed_url }}"
                                        title="YouTube video player"
                                        frameborder="0"
                                        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                                        allowfullscreen></iframe>
                            </div>
                        </div>
                    </div>
                    {% endif %}
                </div>
                
                <p class="mb-3">
                    <strong>URL:</strong> 
                    <a href="{{ tool.url }}" target="_blank" class="text-break">{{ tool.url }}</a>
                </p>
                <div class="btn-group">
                    <a href="{{ url_for('moderate_tool', tool_id=tool.id, action='approve') }}" 
                       class="btn btn-success">Approve</a>
                    <a href="{{ url_for('moderate_tool', tool_id=tool.id, action='reject') }}" 
                       class="btn btn-danger">Reject</a>
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
</div>
{% else %}
<div class="alert alert-info">No tools pending moderation.</div>
{% endif %}
{% endblock %}